# v0.3.3 Observations

Assessed: 2026-03-19
Context: Batch-add DAG awareness and issue body editing

## Sanbao Telemetry

| Metric | Value |
|---|---|
| Issues | 7 |
| MPG | 1.0 commits/issue |
| Speed | 4003 issues/week |
| Lead time | 0.4 hrs avg |
| Cycle time | 0.0 hrs avg |
| Rework rate | 0% |
| Fever | GREEN |
| Time-in-chain | 97% |
| Shadow work | 3% |
| Forecast reliability | HIGH (100% planned issues with session data) |

### Hotspots

| File | Changes | Lines |
|---|---|---|
| internal/cli/issue_edit.go | 2 | 1365 |
| internal/cli/issue_add_test.go | 3 | 373 |
| internal/cli/issue.go | 3 | 271 |

### Assessment

Sanbao produces meaningful output for a small release. The DORA metrics
(lead time, cycle time) reflect the compressed single-session nature of
the work. The hotspot analysis correctly identifies the files with the
most churn. Change coupling (issue.go <-> issue_add_test.go) is expected
for TDD.

The 97% time-in-chain ratio shows that tracking overhead is minimal when
issues are created before starting work.

## Postmortem Findings

1. **Chicken-and-egg with batch-add:** Used the old sequential batch-add
   to create the v0.3.3 issues, requiring manual dependency correction.
   Next time: build and install the binary first, then create issues.

2. **Pre-existing test failure erodes trust:** TestMilestoneComplete_VerifySkippedWithWarning
   has been failing throughout v0.3.2 and v0.3.3 work. Fix before v0.3.4.

3. **Ralph Loop not exercised:** PRD AC required 3+ iterations without
   manual intervention. Feature development used TDD but not Ralph Loop.
   Being addressed as a follow-up task.

4. **MPG of exactly 1.0:** One commit per issue is clean but may indicate
   issue sizing matched to "one commit" rather than logical decomposition.

## Component Verdicts

- **Sanbao telemetry:** WORKS. Produces actionable output for small releases.
- **Postmortem:** WORKS. 4 findings, all specific to v0.3.3 work (not generic).
- **Ralph Loop:** WORKS. 3 iterations on a real Go test fix. Loop correctly
  cycles the prompt, preserves context via files/git. Operator error: launched
  without `--completion-promise` or `--max-iterations`, requiring manual cancel.
  Correct invocation would be `--completion-promise "DONE" --max-iterations 5`
  for finite TDD tasks.
