# v0.2 Implementation Plan

**Design doc:** [2026-03-16-v0.2-implementation-design.md](2026-03-16-v0.2-implementation-design.md)
**Worktree:** `.worktrees/v0.2` (branch `feature/v0.2`)
**Base:** `pu` at `2f07027`

---

## Phase 1: Data Model Enrichment

### Component 1.1: Actor Package

**Files:**
- Create: `internal/actor/actor.go`
- Create: `internal/actor/actor_test.go`

**Tasks:**
- [ ] Write failing test: `ParseActor("human:perigrin")` returns `Actor{Type: "human", ID: "perigrin"}`
- [ ] Run: `go test ./internal/actor/ -run TestParseActor -v` — verify failure
- [ ] Implement `Actor` struct and `ParseActor(s string) Actor`
- [ ] Run: `go test ./internal/actor/ -run TestParseActor -v` — verify pass
- [ ] Write failing test: `ParseActor("agent:claude-code-1")` returns agent type
- [ ] Run test — verify failure
- [ ] Extend `ParseActor` for agent type
- [ ] Run test — verify pass
- [ ] Write failing test: `ParseActor("perigrin")` defaults to `human` type
- [ ] Run test — verify failure
- [ ] Handle unrecognized format (default to human)
- [ ] Run test — verify pass
- [ ] Write failing test: `DeriveActor(name, email string) Actor` infers type from git config values
- [ ] Run test — verify failure
- [ ] Implement `DeriveActor` — heuristic: email containing "agent" or "bot" → agent type, else human
- [ ] Run test — verify pass
- [ ] Write failing test: `Actor.String()` returns `"type:identifier"` format
- [ ] Run test — verify failure
- [ ] Implement `String()` method
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 1.2: Issue Urgency Field

**Files:**
- Modify: `internal/issue/issue.go` — add `Urgency` field to `Issue` struct
- Modify: `internal/issue/issue_test.go` — add parsing/marshal roundtrip tests

**Tasks:**
- [ ] Write failing test: parse issue YAML with `urgency: high` → `Issue.Urgency == UrgencyHigh`
- [ ] Run: `go test ./internal/issue/ -run TestParseUrgency -v` — verify failure
- [ ] Add `Urgency` field (type `Urgency` enum: high, normal, low) and update `Parse()`
- [ ] Run test — verify pass
- [ ] Write failing test: issue without urgency field defaults to `UrgencyNormal`
- [ ] Run test — verify failure
- [ ] Add default in `Parse()`
- [ ] Run test — verify pass
- [ ] Write failing test: `Marshal()` roundtrips urgency correctly
- [ ] Run test — verify failure
- [ ] Update `Marshal()` to include urgency
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 1.3: Transition Log

**Files:**
- Modify: `internal/issue/issue.go` — add `Transition` struct and `Transitions` field
- Modify: `internal/issue/issue_test.go`
- Modify: `internal/issue/state.go` — record transitions on state changes

**Tasks:**
- [ ] Write failing test: parse issue YAML with `transitions` array → `Issue.Transitions` populated
- [ ] Run: `go test ./internal/issue/ -run TestParseTransitions -v` — verify failure
- [ ] Add `Transition` struct (`State string`, `Actor string`, `Timestamp time.Time`) and `Transitions []Transition` field to `Issue`
- [ ] Update `Parse()` to unmarshal transitions
- [ ] Run test — verify pass
- [ ] Write failing test: `Marshal()` roundtrips transitions
- [ ] Run test — verify failure
- [ ] Update `Marshal()` to include transitions
- [ ] Run test — verify pass
- [ ] Write failing test: issue without transitions field → empty slice (backward compat)
- [ ] Run test — verify failure
- [ ] Handle nil/missing transitions in `Parse()`
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 1.4: Reopened State

**Files:**
- Modify: `internal/issue/state.go` — add `Reopened` state and transitions
- Modify: `internal/issue/state_test.go`

**Tasks:**
- [ ] Write failing test: `ValidateTransition(StateDone, "reopen")` returns `StateReopened`
- [ ] Run: `go test ./internal/issue/ -run TestReopenedTransition -v` — verify failure
- [ ] Add `StateReopened` constant, add `done → reopened` to valid transitions
- [ ] Run test — verify pass
- [ ] Write failing test: `ValidateTransition(StateReopened, "start")` returns `StateInProgress`
- [ ] Run test — verify failure
- [ ] Add `reopened → in-progress` to valid transitions
- [ ] Run test — verify pass
- [ ] Write failing test: invalid transitions from reopened (cancel, done directly) are rejected
- [ ] Run test — verify failure
- [ ] Ensure only `reopened → in-progress` is valid
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 1.5: Observed Paths

**Files:**
- Modify: `internal/issue/issue.go` — add `ObservedPaths` field
- Modify: `internal/issue/issue_test.go`

**Tasks:**
- [ ] Write failing test: parse issue YAML with `observed_paths` array → `Issue.ObservedPaths` populated
- [ ] Run: `go test ./internal/issue/ -run TestParseObservedPaths -v` — verify failure
- [ ] Add `ObservedPaths []string` field, update `Parse()`
- [ ] Run test — verify pass
- [ ] Write failing test: `Marshal()` roundtrips observed_paths
- [ ] Run test — verify failure
- [ ] Update `Marshal()`
- [ ] Run test — verify pass
- [ ] Write failing test: missing field → empty slice (backward compat)
- [ ] Run test — verify failure
- [ ] Handle nil/missing in `Parse()`
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 1.6: Steps Section Parsing

**Files:**
- Modify: `internal/issue/sections.go` — add Steps section parsing
- Modify: `internal/issue/sections_test.go`

**Tasks:**
- [ ] Write failing test: `ParseSections()` with `## Steps` section returns `Steps` field with checkbox items
- [ ] Run: `go test ./internal/issue/ -run TestParseSteps -v` — verify failure
- [ ] Add `Steps []string` to `Sections` struct, parse `## Steps` in `ParseSections()`
- [ ] Run test — verify pass
- [ ] Write failing test: Steps section between Context and AC is correctly ordered
- [ ] Run test — verify failure
- [ ] Ensure section parser handles the ordering
- [ ] Run test — verify pass
- [ ] Write failing test: issue without Steps section → empty slice
- [ ] Run test — verify failure
- [ ] Handle missing Steps
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 1.7: AC Subsections

**Files:**
- Modify: `internal/issue/sections.go` — parse Positive/Negative subsections
- Modify: `internal/issue/sections_test.go`

**Tasks:**
- [ ] Write failing test: `ParseSections()` with `### Positive Scenarios` and `### Negative Scenarios` returns separate fields
- [ ] Run: `go test ./internal/issue/ -run TestParseACSubsections -v` — verify failure
- [ ] Add `PositiveScenarios []string` and `NegativeScenarios []string` to `Sections`, parse subsections
- [ ] Run test — verify pass
- [ ] Write failing test: flat AC list (no subsections, v0.1 format) → all items in PositiveScenarios
- [ ] Run test — verify failure
- [ ] Handle flat list as positive scenarios
- [ ] Run test — verify pass
- [ ] Write failing test: `--format json` exposes positive and negative as separate arrays
- [ ] Run test — verify failure
- [ ] Update JSON presentation struct
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 1.8: Session Wiring Fix

**Files:**
- Modify: `internal/cli/issue_edit.go` (or wherever state transitions handle sessions)
- Modify: `internal/cli/issue_edit_test.go`

**Tasks:**
- [ ] Write failing test: `--state start` creates a new session with `start_sha` = current HEAD
- [ ] Run: `go test ./internal/cli/ -run TestStartCreatesSession -v` — verify failure
- [ ] Wire `--state start` to create session record with `StartSHA` and `StartedAt`
- [ ] Run test — verify pass
- [ ] Write failing test: `--state pause` closes current session with `end_sha` and `ended_at`
- [ ] Run test — verify failure
- [ ] Wire `--state pause` to close session
- [ ] Run test — verify pass
- [ ] Write failing test: `--state resume` creates a new session
- [ ] Run test — verify failure
- [ ] Wire `--state resume` to create session
- [ ] Run test — verify pass
- [ ] Write failing test: `--state done` closes current session and records `observed_paths` via `git diff --name-only`
- [ ] Run test — verify failure
- [ ] Wire `--state done` to close session and record observed_paths
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 1.9: Transition Recording in CLI

**Files:**
- Modify: `internal/cli/issue_edit.go` — append transition on every state change
- Modify: `internal/cli/issue_edit_test.go`

**Tasks:**
- [ ] Write failing test: `--state start` appends a transition with actor from git config
- [ ] Run: `go test ./internal/cli/ -run TestStartRecordsTransition -v` — verify failure
- [ ] Read git author from `Store`, call `actor.DeriveActor()`, append `Transition` to issue
- [ ] Run test — verify pass
- [ ] Write failing test: `--state done` appends a transition
- [ ] Run test — verify failure
- [ ] Wire done transition recording
- [ ] Run test — verify pass
- [ ] Write failing test: `--state reopen` appends a transition
- [ ] Run test — verify failure
- [ ] Wire reopen transition recording
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 1.10: Milestone Body (Frontmatter + Markdown)

**Files:**
- Modify: `internal/milestone/milestone.go` — add `Body`, `State`, `Completed` fields; switch to frontmatter+markdown format
- Modify: `internal/milestone/milestone_test.go`

**Tasks:**
- [ ] Write failing test: `Parse()` reads YAML frontmatter + markdown body from milestone content
- [ ] Run: `go test ./internal/milestone/ -run TestParseMilestoneBody -v` — verify failure
- [ ] Add `Body string`, `State string` (default "open"), `Completed *time.Time` fields
- [ ] Rewrite `Parse()` to use frontmatter+body pattern (like issue.Parse)
- [ ] Run test — verify pass
- [ ] Write failing test: `Parse()` handles pure YAML input (v0.1 backward compat)
- [ ] Run test — verify failure
- [ ] Detect pure YAML vs frontmatter+body in `Parse()`
- [ ] Run test — verify pass
- [ ] Write failing test: `Marshal()` produces frontmatter+body output
- [ ] Run test — verify failure
- [ ] Rewrite `Marshal()` for new format
- [ ] Run test — verify pass
- [ ] Write failing test: roundtrip with Context, File Structure, Design Rationale sections
- [ ] Run test — verify failure
- [ ] Ensure body preservation through parse/marshal cycle
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 1.11: Milestone JSON with Issue Show

**Files:**
- Modify: `internal/cli/issue_show.go` — embed milestone body in issue JSON output
- Modify: `internal/cli/issue_show_test.go`

**Tasks:**
- [ ] Write failing test: `issue show --format json` includes `milestone_context` field with milestone body
- [ ] Run: `go test ./internal/cli/ -run TestIssueShowIncludesMilestoneContext -v` — verify failure
- [ ] Load parent milestone in issue show path, embed body in JSON struct
- [ ] Run test — verify pass
- [ ] Write failing test: issue with no milestone → `milestone_context` is empty/null
- [ ] Run test — verify failure
- [ ] Handle missing milestone gracefully
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

**Phase 1 verification:** `go test ./... -race` — all packages pass

---

## Phase 2: Milestone Completion Gates

### Component 2.1: Resolution Command Execution

**Files:**
- Modify: `internal/cli/milestone_edit.go` — add `--resolve` flag
- Modify: `internal/cli/milestone_edit_test.go`

**Tasks:**
- [ ] Write failing test: `milestone edit v0.1 --resolve` reads resolution field, executes as shell command, reports pass
- [ ] Run: `go test ./internal/cli/ -run TestMilestoneResolve -v` — verify failure
- [ ] Implement `--resolve` flag: read milestone, execute `resolution` via `os/exec`, report stdout/stderr
- [ ] Run test — verify pass
- [ ] Write failing test: resolution command failure → non-zero exit, error message
- [ ] Run test — verify failure
- [ ] Handle command failure with diagnostic output
- [ ] Run test — verify pass
- [ ] Write failing test: milestone with no resolution field → error "no resolution command configured"
- [ ] Run test — verify failure
- [ ] Check for empty resolution field
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 2.2: Completion Gate Enforcement

**Files:**
- Modify: `internal/cli/milestone_edit.go` — gate `--state complete`
- Modify: `internal/cli/milestone_edit_test.go`

**Tasks:**
- [ ] Write failing test: `--state complete` with all issues done → succeeds, sets state and completed timestamp
- [ ] Run: `go test ./internal/cli/ -run TestMilestoneComplete -v` — verify failure
- [ ] Implement: load all issues in milestone, check all done/cancelled, set state to "completed"
- [ ] Run test — verify pass
- [ ] Write failing test: `--state complete` with pending issues → refuses with list of blocking issues
- [ ] Run test — verify failure
- [ ] Add issue state gate check
- [ ] Run test — verify pass
- [ ] Write failing test: `--state complete` runs resolution command as gate (fails if command fails)
- [ ] Run test — verify failure
- [ ] Wire resolution execution into completion flow
- [ ] Run test — verify pass
- [ ] Write failing test: completed milestone rejects `issue edit --milestone <completed>`
- [ ] Run test — verify failure
- [ ] Add completed milestone check in issue edit
- [ ] Run test — verify pass
- [ ] Write failing test: verify gate skipped with warning when `git-zhi-verify` not on `$PATH`
- [ ] Run test — verify failure
- [ ] Check for verify binary, skip with warning if absent
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

**Phase 2 verification:** `go test ./... -race` — all packages pass

---

## Phase 3: git-zhi-verify Plugin

### Component 3.1: AC Command Extraction

**Files:**
- Create: `internal/verify/extract.go`
- Create: `internal/verify/extract_test.go`
- Modify: `internal/issue/sections.go` — add method to extract backtick commands from AC items

**Tasks:**
- [ ] Write failing test: `ExtractCommands(sections)` returns commands from backtick-delimited strings in Positive/Negative Scenarios
- [ ] Run: `go test ./internal/verify/ -run TestExtractCommands -v` — verify failure
- [ ] Implement `ExtractCommands` — regex for backtick-wrapped commands in AC items
- [ ] Run test — verify pass
- [ ] Write failing test: flat AC list (no subsections) → all commands tagged as positive
- [ ] Run test — verify failure
- [ ] Handle flat format
- [ ] Run test — verify pass
- [ ] Write failing test: AC item without backtick command → skipped
- [ ] Run test — verify failure
- [ ] Skip non-command items
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 3.2: Command Executor

**Files:**
- Create: `internal/verify/executor.go`
- Create: `internal/verify/executor_test.go`

**Tasks:**
- [ ] Write failing test: `Execute(cmd, repoRoot, timeout)` runs command, returns exit code + stdout + stderr
- [ ] Run: `go test ./internal/verify/ -run TestExecute -v` — verify failure
- [ ] Implement `Execute` via `os/exec.CommandContext` with timeout
- [ ] Run test — verify pass
- [ ] Write failing test: command exceeding timeout → killed, reported as failure
- [ ] Run test — verify failure
- [ ] Wire context deadline for timeout
- [ ] Run test — verify pass
- [ ] Write failing test: command returning non-zero → captured as failure with output
- [ ] Run test — verify failure
- [ ] Capture exit code from `exec.ExitError`
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 3.3: Prioritized Ordering

**Files:**
- Create: `internal/verify/priority.go`
- Create: `internal/verify/priority_test.go`

**Tasks:**
- [ ] Write failing test: issues with `observed_paths` overlapping recent changes sorted first
- [ ] Run: `go test ./internal/verify/ -run TestPriorityOrder -v` — verify failure
- [ ] Implement: compute `git diff --name-only <done_sha>..HEAD`, intersect with observed_paths, sort by overlap
- [ ] Run test — verify pass
- [ ] Write failing test: issues without overlap sorted by topological order
- [ ] Run test — verify failure
- [ ] Fall back to topo sort for non-overlapping issues
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 3.4: Verify CLI

**Files:**
- Create: `cmd/git-zhi-verify/main.go`
- Create: `internal/verify/cmd.go` — Cobra command wiring
- Create: `internal/verify/cmd_test.go`

**Tasks:**
- [ ] Write failing test: `git-zhi-verify v0.1` loads milestone, runs all AC commands, reports results
- [ ] Run: `go test ./internal/verify/ -run TestVerifyCLI -v` — verify failure
- [ ] Wire Cobra command: load store, load milestone issues, extract commands, execute in priority order, format output
- [ ] Run test — verify pass
- [ ] Write failing test: `--fail-fast` stops on first failure
- [ ] Run test — verify failure
- [ ] Add fail-fast flag
- [ ] Run test — verify pass
- [ ] Write failing test: `--dry-run` lists commands without executing
- [ ] Run test — verify failure
- [ ] Add dry-run flag
- [ ] Run test — verify pass
- [ ] Write failing test: `--format json` returns structured results
- [ ] Run test — verify failure
- [ ] Add JSON output format
- [ ] Run test — verify pass
- [ ] Write failing test: exit code 0 on all pass, 1 on any failure
- [ ] Run test — verify failure
- [ ] Set exit code based on results
- [ ] Run test — verify pass
- [ ] Write `cmd/git-zhi-verify/main.go` entry point
- [ ] Run: `go build -o git-zhi-verify ./cmd/git-zhi-verify/` — verify builds
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

**Phase 3 verification:** `go build ./cmd/git-zhi-verify/ && go test ./... -race`

---

## Phase 4: Parallelization

### Component 4.1: Path Overlap Detection

**Files:**
- Create: `internal/graph/paths.go`
- Create: `internal/graph/paths_test.go`

**Tasks:**
- [ ] Write failing test: `PathsOverlap(["internal/parser/sig.go"], ["internal/parser/err.go"])` → true (shared directory)
- [ ] Run: `go test ./internal/graph/ -run TestPathsOverlap -v` — verify failure
- [ ] Implement `PathsOverlap(a, b []string) bool` — file-level and directory-level comparison
- [ ] Run test — verify pass
- [ ] Write failing test: `PathsOverlap(["internal/parser/sig.go"], ["internal/config/cfg.go"])` → false
- [ ] Run test — verify failure
- [ ] Ensure non-overlapping paths return false
- [ ] Run test — verify pass
- [ ] Write failing test: exact file match detected
- [ ] Run test — verify failure
- [ ] Handle exact file overlap
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 4.2: Parallel Assignment

**Files:**
- Modify: `internal/graph/graph.go` — add `ParallelAssignment` method
- Modify: `internal/graph/graph_test.go`

**Tasks:**
- [ ] Write failing test: `ParallelAssignment(ready, 2, pathsFn)` returns two non-overlapping groups
- [ ] Run: `go test ./internal/graph/ -run TestParallelAssignment -v` — verify failure
- [ ] Implement: iterate ready set in critical chain order, assign to groups if no path overlap with existing group members
- [ ] Run test — verify pass
- [ ] Write failing test: all ready issues have overlapping paths → single group
- [ ] Run test — verify failure
- [ ] Handle full overlap case
- [ ] Run test — verify pass
- [ ] Write failing test: worker count exceeds ready set → groups limited by ready set size
- [ ] Run test — verify failure
- [ ] Cap groups at ready set size
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 4.3: Per-Actor HEAD Resolution

**Files:**
- Modify: `internal/graph/graph.go` — update `Head()` to accept optional actor filter
- Modify: `internal/graph/graph_test.go`

**Tasks:**
- [ ] Write failing test: `Head("")` returns any in-progress issue (v0.1 compat)
- [ ] Run: `go test ./internal/graph/ -run TestHeadNoActor -v` — verify failure (if signature changed)
- [ ] Update `Head()` signature to accept actor string (empty = v0.1 behavior)
- [ ] Run test — verify pass
- [ ] Write failing test: `Head("agent:claude-code-1")` returns that actor's in-progress issue
- [ ] Run test — verify failure
- [ ] Filter in-progress issues by last transition actor
- [ ] Run test — verify pass
- [ ] Write failing test: `Head("agent:claude-code-1")` with no in-progress issue → picks from ready set excluding other workers' in-progress issues
- [ ] Run test — verify failure
- [ ] Implement ready-set-minus-other-workers logic
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 4.4: Ready Set CLI and Forecasting

**Files:**
- Modify: `internal/cli/chain_list.go` — add `--ready` flag
- Modify: `internal/cli/chain_list_test.go`
- Modify: `internal/cli/milestone_show.go` — add `--workers` flag
- Modify: `internal/cli/milestone_show_test.go`
- Modify: `internal/cli/next.go` — add `--actor` flag
- Modify: `internal/cli/next_test.go`

**Tasks:**
- [ ] Write failing test: `list --ready` shows ready set with path analysis output
- [ ] Run: `go test ./internal/cli/ -run TestListReady -v` — verify failure
- [ ] Implement `--ready` flag: call `ReadySet()`, run path overlap, format output
- [ ] Run test — verify pass
- [ ] Write failing test: `list --ready --format json` returns structured ready set with overlap info
- [ ] Run test — verify failure
- [ ] Add JSON output for ready set
- [ ] Run test — verify pass
- [ ] Write failing test: `milestone show v0.1 --workers 2` shows forecast
- [ ] Run test — verify failure
- [ ] Implement forecast: sequential time, parallel speedup, coordination tax, floor
- [ ] Run test — verify pass
- [ ] Write failing test: `next --actor "agent:claude-code-1"` uses per-actor HEAD
- [ ] Run test — verify failure
- [ ] Wire `--actor` flag to `Head()` call
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

**Phase 4 verification:** `go test ./... -race` — all packages pass

---

## Phase 5: Lineage Tracking

### Component 5.1: Blame-to-Issue Mapper

**Files:**
- Create: `internal/lineage/lineage.go`
- Create: `internal/lineage/lineage_test.go`

**Tasks:**
- [ ] Write failing test: `BuildSessionIndex(issues)` creates map of `{SHA → issueID}` from session ranges
- [ ] Run: `go test ./internal/lineage/ -run TestBuildSessionIndex -v` — verify failure
- [ ] Implement: iterate done issues, for each session range map SHAs to issue ID
- [ ] Run test — verify pass
- [ ] Write failing test: `ComputeLineage(repo, files, sessionIndex)` runs `git blame`, maps blame SHAs to issues, returns `[]LineageEntry`
- [ ] Run test — verify failure
- [ ] Implement: run blame per file, look up each line's SHA in session index, aggregate by issue
- [ ] Run test — verify pass
- [ ] Write failing test: file with no blame matches (new file) → empty lineage
- [ ] Run test — verify failure
- [ ] Handle no-match case
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 5.2: Lineage in Issue Show

**Files:**
- Modify: `internal/cli/issue_show.go` — add lineage section to human and JSON output
- Modify: `internal/cli/issue_show_test.go`

**Tasks:**
- [ ] Write failing test: `issue show` with done upstream issues displays "Upstream lineage" section
- [ ] Run: `go test ./internal/cli/ -run TestIssueShowLineage -v` — verify failure
- [ ] Compute lineage in issue show path, format as human-readable section
- [ ] Run test — verify pass
- [ ] Write failing test: `--format json` includes lineage with upstream issue titles, AC, and context
- [ ] Run test — verify failure
- [ ] Add lineage to JSON presentation struct
- [ ] Run test — verify pass
- [ ] Write failing test: issue with no context.paths → lineage section omitted
- [ ] Run test — verify failure
- [ ] Skip lineage computation when no paths declared
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 5.3: Lineage-Informed Verify Ordering

**Files:**
- Modify: `internal/verify/priority.go` — add tier 2 lineage ordering
- Modify: `internal/verify/priority_test.go`

**Tasks:**
- [ ] Write failing test: issues with lineage connections to recently done issues sorted into tier 2
- [ ] Run: `go test ./internal/verify/ -run TestPriorityWithLineage -v` — verify failure
- [ ] Compute lineage for done issues, use as secondary sort criterion
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

**Phase 5 verification:** `go test ./... -race` — all packages pass

---

## Phase 6: Planning Telemetry

### Component 6.1: Forecast Accuracy Tracking

**Files:**
- Modify: `internal/milestone/milestone.go` — add `ForecastHistory` field
- Modify: `internal/milestone/milestone_test.go`
- Modify: `internal/telemetry/telemetry.go` — add forecast accuracy computation
- Modify: `internal/telemetry/telemetry_test.go`

**Tasks:**
- [ ] Write failing test: milestone with `forecast_history` entries → parsed correctly
- [ ] Run: `go test ./internal/milestone/ -run TestForecastHistory -v` — verify failure
- [ ] Add `ForecastHistory` struct and field, update `Parse()`/`Marshal()`
- [ ] Run test — verify pass
- [ ] Write failing test: `ComputeForecastAccuracy(predicted, actual)` returns ratio
- [ ] Run: `go test ./internal/telemetry/ -run TestForecastAccuracy -v` — verify failure
- [ ] Implement in telemetry package
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 6.2: Parallel Efficiency and Ready Set Width

**Files:**
- Modify: `internal/telemetry/telemetry.go` — add parallel efficiency, ready set width metrics
- Modify: `internal/telemetry/telemetry_test.go`

**Tasks:**
- [ ] Write failing test: `ComputeParallelEfficiency(actual, theoretical)` returns ratio
- [ ] Run: `go test ./internal/telemetry/ -run TestParallelEfficiency -v` — verify failure
- [ ] Implement
- [ ] Run test — verify pass
- [ ] Write failing test: `Stats` struct includes `ReadySetWidth` and `PathOverlapCount` fields
- [ ] Run test — verify failure
- [ ] Add fields, compute from graph data
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

**Phase 6 verification:** `go test ./... -race` — all packages pass

---

## Phase 7: git-zhi-sanbao Plugin

### Component 7.1: DORA Metrics

**Files:**
- Create: `internal/sanbao/dora/dora.go`
- Create: `internal/sanbao/dora/dora_test.go`

**Tasks:**
- [ ] Write failing test: `LeadTime(issue)` returns duration from created to done transition
- [ ] Run: `go test ./internal/sanbao/dora/ -run TestLeadTime -v` — verify failure
- [ ] Implement: find `done` transition, subtract `created` timestamp
- [ ] Run test — verify pass
- [ ] Write failing test: `CycleTime(issue)` returns duration from start to done
- [ ] Run test — verify failure
- [ ] Implement: find `start` and `done` transitions
- [ ] Run test — verify pass
- [ ] Write failing test: `ReworkRate(issues)` returns percentage of done→reopened
- [ ] Run test — verify failure
- [ ] Implement: count reopened transitions, divide by total done
- [ ] Run test — verify pass
- [ ] Write failing test: `CompletionFrequency(issues, period)` returns issues per period
- [ ] Run test — verify failure
- [ ] Implement
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 7.2: SPACE Metrics

**Files:**
- Create: `internal/sanbao/space/space.go`
- Create: `internal/sanbao/space/space_test.go`

**Tasks:**
- [ ] Write failing test: `AgentAutonomyRate(issues)` returns percentage of issues completed entirely by agents
- [ ] Run: `go test ./internal/sanbao/space/ -run TestAgentAutonomy -v` — verify failure
- [ ] Implement: check all transitions per issue, count where all actors are agents
- [ ] Run test — verify pass
- [ ] Write failing test: `ReviewCycleCount(issue)` returns commits between first done attempt and final done
- [ ] Run test — verify failure
- [ ] Implement
- [ ] Run test — verify pass
- [ ] Write failing test: remaining SPACE dimensions (milestone completion rate, forecast accuracy, MPG, session count, time-in-chain, shadow work)
- [ ] Implement each
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 7.3: CALMS Indicators

**Files:**
- Create: `internal/sanbao/calms/calms.go`
- Create: `internal/sanbao/calms/calms_test.go`

**Tasks:**
- [ ] Write failing test: `VerificationCoverage(issues)` returns percentage with executable AC commands
- [ ] Run: `go test ./internal/sanbao/calms/ -run TestVerificationCoverage -v` — verify failure
- [ ] Implement
- [ ] Run test — verify pass
- [ ] Write failing test: `WIPCompliance(issues)` counts instances of >1 concurrent in-progress
- [ ] Run test — verify failure
- [ ] Implement
- [ ] Run test — verify pass
- [ ] Write failing tests for remaining CALMS indicators (scope cut frequency, metric completeness, postmortem completion rate)
- [ ] Implement each
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 7.4: VADER Sentiment

**Files:**
- Create: `internal/sanbao/sentiment/sentiment.go`
- Create: `internal/sanbao/sentiment/sentiment_test.go`

**Tasks:**
- [ ] Add VADER Go library to `go.mod`
- [ ] Write failing test: `AnalyzeCommitSentiment(message)` returns compound score
- [ ] Run: `go test ./internal/sanbao/sentiment/ -run TestAnalyzeCommit -v` — verify failure
- [ ] Implement wrapper around VADER library
- [ ] Run test — verify pass
- [ ] Write failing test: `IssueTrajectory(repo, issue)` returns sequence of compound scores for commits in session ranges
- [ ] Run test — verify failure
- [ ] Implement: walk commits in session ranges, score each message
- [ ] Run test — verify pass
- [ ] Write failing test: `DetectAnomalies(trajectory)` flags compound < -0.1 for 3+ consecutive commits
- [ ] Run test — verify failure
- [ ] Implement anomaly detection
- [ ] Run test — verify pass
- [ ] Write failing test: `MilestoneSentiment(issues)` returns aggregate
- [ ] Run test — verify failure
- [ ] Implement
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 7.5: Issue Difficulty

**Files:**
- Create: `internal/sanbao/difficulty/difficulty.go`
- Create: `internal/sanbao/difficulty/difficulty_test.go`

**Tasks:**
- [ ] Write failing test: `ComputeDifficulty(issue, stats)` returns 0-1 normalized score
- [ ] Run: `go test ./internal/sanbao/difficulty/ -run TestComputeDifficulty -v` — verify failure
- [ ] Implement: weighted combination of MPG, cycle time, reopens, sentiment, session count
- [ ] Run test — verify pass
- [ ] Write failing test: issue with high MPG and negative sentiment scores higher than simple issue
- [ ] Run test — verify failure
- [ ] Tune weights
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 7.6: Code Complexity (Language-Agnostic)

**Files:**
- Create: `internal/sanbao/complexity/complexity.go`
- Create: `internal/sanbao/complexity/complexity_test.go`

**Tasks:**
- [ ] Write failing test: `FileChurn(repo, sessions)` returns modification count per file
- [ ] Run: `go test ./internal/sanbao/complexity/ -run TestFileChurn -v` — verify failure
- [ ] Implement via `git log --name-only` across session ranges
- [ ] Run test — verify pass
- [ ] Write failing test: `ChurnSizeHotspots(churn, sizes)` returns ranked product
- [ ] Run test — verify failure
- [ ] Implement
- [ ] Run test — verify pass
- [ ] Write failing test: `ChangeCoupling(commits)` returns co-occurrence matrix
- [ ] Run test — verify failure
- [ ] Implement
- [ ] Run test — verify pass
- [ ] Write failing test: `IndentationDepth(file)` returns avg and max nesting
- [ ] Run test — verify failure
- [ ] Implement: scan leading whitespace
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 7.7: LSP Integration

**Files:**
- Create: `internal/sanbao/lsp/client.go`
- Create: `internal/sanbao/lsp/client_test.go`

**Tasks:**
- [ ] Write failing test: `NewLSPClient("gopls")` spawns server, sends `initialize`, receives capabilities
- [ ] Run: `go test ./internal/sanbao/lsp/ -run TestLSPInit -v` — verify failure
- [ ] Implement JSON-RPC client: spawn process, send initialize, parse response
- [ ] Run test — verify pass
- [ ] Write failing test: `Diagnostics(file)` returns diagnostic list from language server
- [ ] Run test — verify failure
- [ ] Implement `textDocument/diagnostic` request
- [ ] Run test — verify pass
- [ ] Write failing test: `Shutdown()` gracefully terminates server
- [ ] Run test — verify failure
- [ ] Implement shutdown/exit sequence
- [ ] Run test — verify pass
- [ ] Write failing test: server not found → clear error, no panic
- [ ] Run test — verify failure
- [ ] Handle missing binary
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 7.8: Sanbao Report CLI

**Files:**
- Create: `cmd/git-zhi-sanbao/main.go`
- Create: `internal/sanbao/report/report.go`
- Create: `internal/sanbao/report/report_test.go`

**Tasks:**
- [ ] Write failing test: `GenerateReport(store, milestone)` aggregates all domains into report struct
- [ ] Run: `go test ./internal/sanbao/report/ -run TestGenerateReport -v` — verify failure
- [ ] Wire all sanbao sub-packages into report generation
- [ ] Run test — verify pass
- [ ] Write failing test: `--format json` returns structured report
- [ ] Run test — verify failure
- [ ] Add JSON formatter
- [ ] Run test — verify pass
- [ ] Write failing test: `--domain dora` filters to DORA metrics only
- [ ] Run test — verify failure
- [ ] Add domain filter
- [ ] Run test — verify pass
- [ ] Write `cmd/git-zhi-sanbao/main.go` entry point
- [ ] Run: `go build -o git-zhi-sanbao ./cmd/git-zhi-sanbao/` — verify builds
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

**Phase 7 verification:** `go build ./cmd/git-zhi-sanbao/ && go test ./... -race`

---

## Phase 8: git-zhi-docs Plugin

### Component 8.1: Directory Scaffolding and Lazy Init

**Files:**
- Create: `internal/docs/scaffold.go`
- Create: `internal/docs/scaffold_test.go`

**Tasks:**
- [ ] Write failing test: `Scaffold(repoRoot)` creates canonical `docs/` directory structure
- [ ] Run: `go test ./internal/docs/ -run TestScaffold -v` — verify failure
- [ ] Implement: create dirs, create starter content for living documents (CONTRIBUTING.md, coding-conventions.md, development-workflow.md)
- [ ] Run test — verify pass
- [ ] Write failing test: `Scaffold()` with existing `docs/` → creates only missing dirs
- [ ] Run test — verify failure
- [ ] Check existence before creating
- [ ] Run test — verify pass
- [ ] Write failing test: `Scaffold()` with existing CONTRIBUTING.md → proposes additions, preserves content
- [ ] Run test — verify failure
- [ ] Detect existing CONTRIBUTING.md, append "Short Links" section if missing
- [ ] Run test — verify pass
- [ ] Write failing test: idempotent — running twice produces same result
- [ ] Run test — verify failure
- [ ] Ensure idempotency
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 8.2: Structural Validation (docs check)

**Files:**
- Create: `internal/docs/check.go`
- Create: `internal/docs/check_test.go`

**Tasks:**
- [ ] Write failing test: `Check(repoRoot)` validates all docs/ files reachable from CONTRIBUTING.md links
- [ ] Run: `go test ./internal/docs/ -run TestCheck -v` — verify failure
- [ ] Implement: parse CONTRIBUTING.md for links, walk docs/, report unreachable files
- [ ] Run test — verify pass
- [ ] Write failing test: detect dead links between documents
- [ ] Run test — verify failure
- [ ] Implement inter-document link validation
- [ ] Run test — verify pass
- [ ] Write failing test: ADR numbering validation (sequential, no gaps)
- [ ] Run test — verify failure
- [ ] Implement ADR numbering check
- [ ] Run test — verify pass
- [ ] Write failing test: `covers` frontmatter paths resolve to existing files/dirs
- [ ] Run test — verify failure
- [ ] Implement covers path validation
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 8.3: Churn-Relative Health

**Files:**
- Create: `internal/docs/health.go`
- Create: `internal/docs/health_test.go`

**Tasks:**
- [ ] Write failing test: `Health(repoRoot)` computes drift for docs with `covers` frontmatter
- [ ] Run: `go test ./internal/docs/ -run TestHealth -v` — verify failure
- [ ] Implement: parse frontmatter, `git log --since=<date> --name-only -- <paths>`, classify drift
- [ ] Run test — verify pass
- [ ] Write failing test: stability metadata modulates sensitivity (experimental → flagged sooner)
- [ ] Run test — verify failure
- [ ] Apply stability weighting
- [ ] Run test — verify pass
- [ ] Write failing test: ADRs and postmortems exempt from staleness
- [ ] Run test — verify failure
- [ ] Skip ADR and postmortem files
- [ ] Run test — verify pass
- [ ] Write failing test: coverage gap detection (packages with no architecture doc)
- [ ] Run test — verify failure
- [ ] Implement: scan internal/ packages, check for corresponding docs
- [ ] Run test — verify pass
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

### Component 8.4: Docs CLI

**Files:**
- Create: `cmd/git-zhi-docs/main.go`
- Create: `internal/docs/cmd.go`
- Create: `internal/docs/cmd_test.go`

**Tasks:**
- [ ] Write failing test: `docs init` scaffolds directory structure (calls Scaffold)
- [ ] Run: `go test ./internal/docs/ -run TestDocsCLI -v` — verify failure
- [ ] Wire Cobra commands: init, check, health
- [ ] Run test — verify pass
- [ ] Write failing test: `docs check` and `docs health` lazy-init if no `docs/` dir exists
- [ ] Run test — verify failure
- [ ] Add lazy init before check/health execution
- [ ] Run test — verify pass
- [ ] Write failing test: `--format json` on all three commands
- [ ] Run test — verify failure
- [ ] Add JSON formatters
- [ ] Run test — verify pass
- [ ] Write `cmd/git-zhi-docs/main.go` entry point
- [ ] Run: `go build -o git-zhi-docs ./cmd/git-zhi-docs/` — verify builds
- [ ] Run full suite: `go test ./... -race`
- [ ] Commit

**Phase 8 verification:** `go build ./cmd/git-zhi-docs/ && go test ./... -race`

---

## Phase 9: Crochet (Separate Repo)

*This phase is executed in a separate repo: `github.com/perigrin/crochet`*

### Component 9.1: Crochet Repo Bootstrap

**Tasks:**
- [ ] Create repo `github.com/perigrin/crochet`
- [ ] Set up skill directory structure: `skills/refinement/`, `skills/postmortem/`
- [ ] Write README.md with installation and usage
- [ ] Commit

### Component 9.2: crochet:refinement Skill

**Files:**
- Create: `skills/refinement/refinement.md` — skill definition
- Create: `skills/refinement/architect-prompt.md` — architect agent system prompt
- Create: `skills/refinement/decomposer-prompt.md` — decomposer agent system prompt
- Create: `skills/refinement/sqe-prompt.md` — SQE agent system prompt
- Create: `skills/refinement/techwriter-prompt.md` — technical writer agent system prompt

**Tasks:**
- [ ] Write skill definition: trigger conditions, input (spec file path), output format
- [ ] Write architect agent prompt: reads spec + codebase, produces milestone context, calls `git zhi milestone add`
- [ ] Write decomposer agent prompt: breaks spec into issues with deps, context, Steps, positive ACs, calls `git zhi issue add`
- [ ] Write SQE agent prompt: reads positive ACs, generates negative scenarios, calls `git zhi issue edit`
- [ ] Write technical writer agent prompt: reads spec + `git zhi docs health --format json`, adds doc steps to code issues, creates standalone doc issues
- [ ] Write lazy init logic: check for CONTRIBUTING.md/CLAUDE.md, bootstrap if needed, call `git zhi docs init`
- [ ] Test refinement end-to-end against a sample spec in the git-zhi repo
- [ ] Commit

### Component 9.3: crochet:postmortem Skill

**Files:**
- Create: `skills/postmortem/postmortem.md` — skill definition

**Tasks:**
- [ ] Write skill definition: trigger (milestone completion), inputs (all via `git zhi` CLI JSON), output format
- [ ] Write postmortem template: four questions (what worked, what didn't, what puzzles, what to change)
- [ ] Define data gathering: issue list, milestone show, verify results, docs health, sanbao report (if available), git log
- [ ] Define output: markdown attached via `git zhi milestone edit --postmortem`
- [ ] Test postmortem end-to-end against a completed milestone
- [ ] Commit

**Phase 9 verification:** End-to-end test of refinement + postmortem against git-zhi itself

---

## Dependency Graph

```
Phase 1 (Data Model) ──→ Phase 2 (Gates) ──→ Phase 3 (Verify)
     │                                              │
     ├──→ Phase 4 (Parallelization) ────────────────┤
     │                                              │
     ├──→ Phase 5 (Lineage) ────────────────────────┤
     │         │                                    │
     │         ├──→ Phase 7 (Sanbao)                │
     │         │                                    │
     └──→ Phase 6 (Telemetry) ←─── Phase 4, 5      │
                                                    │
Phase 8 (Docs) ─── [no strict dependency] ──────────┤
                                                    │
                                                    ▼
                                        Phase 9 (Crochet)
                                        [depends on all]
```

**Parallel opportunities:**
- Phase 4 (Parallelization) and Phase 5 (Lineage) can run in parallel after Phase 1
- Phase 8 (Docs) can start any time
- Phase 7 (Sanbao) can start after Phase 1 + 5
